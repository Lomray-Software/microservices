enum InputType {
  TEXT = 'text',
  NUMBER = 'number',
  RICH_TEXT = 'richText',
  DATE = 'date',
  BOOLEAN = 'boolean',
  EMAIL = 'email',
  PASSWORD = 'password',
  ENUM = 'enum',
  JSON = 'json',
  RELATION = 'relation',
  COMPONENT = 'component',
}

export default InputType;
