enum RefundStatus {
  INITIAL = 'initial',
  IN_PROCESS = 'inProcess',
  REQUIRES_ACTION = 'requiresAction',
  SUCCESS = 'success',
  ERROR = 'error',
  CANCELED = 'canceled',
}

export default RefundStatus;
