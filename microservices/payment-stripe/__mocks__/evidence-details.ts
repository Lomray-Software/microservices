import { disputeMock } from './dispute';

const evidenceDetailsMock = {
  disputeId: disputeMock.id,
  hasEvidence: false,
  submissionCount: 0,
  dueBy: null,
  isPastBy: false,
};

// eslint-disable-next-line import/prefer-default-export
export { evidenceDetailsMock };
